services:
  db:
    image: postgres:18-alpine
    container_name: wms-be-db
    environment:
      POSTGRES_USER: wms
      POSTGRES_PASSWORD: wms
      POSTGRES_DB: wms
    ports:
      - '55432:5432'
